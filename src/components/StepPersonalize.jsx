// src/components/StepPersonalize.jsx — Etapa 3 (personalização da descoberta).
'use client'
import DiscoveryPersonalize from '@/components/discovery/DiscoveryPersonalize'
export default function StepPersonalize(props) {
  return <DiscoveryPersonalize {...props} />
}
